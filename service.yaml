apiVersion: v1
kind: Service
metadata:
  name: wisecow-service
spec:
  selector:
    app: wisecow
  ports:
    - protocol: TCP
      port: 80           # Exposed port
      targetPort: 4499   # Container port
  type: LoadBalancer
